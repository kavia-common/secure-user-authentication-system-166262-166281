{"is_source_file": true, "format": "SQL", "description": "This SQL script manages the migration and setup of a unified users table in a Supabase postgresql database. It drops legacy tables and types, creates a new 'public.users' table with fields for authentication and verification, sets triggers for maintaining timestamps, adds indexes, and configures row level security policies.", "external_files": ["information_schema.tables", "information_schema.schemata", "pg_type", "pg_namespace"], "external_methods": ["gen_random_uuid()", "auth.email()"], "published": ["public.users", "public.set_updated_at"], "classes": [], "methods": [{"name": "public.set_updated_at", "description": "Trigger function to automatically update the 'updated_at' timestamp on row modification."}], "calls": ["create schema if not exists public", "drop table if exists app.verification_codes cascade", "drop table if exists app.profiles cascade", "drop type app.code_purpose", "drop schema app", "create table if not exists public.users", "create or replace function public.set_updated_at", "drop trigger if exists trg_users_set_updated_at on public.users", "create trigger trg_users_set_updated_at", "create index if not exists idx_users_email", "create index if not exists idx_users_verification_expiry", "create index if not exists idx_users_reset_expiry", "alter table public.users enable row level security", "drop policy if exists \"Users: no select for anon\" on public.users", "drop policy if exists \"Users: owner can select self\" on public.users", "drop policy if exists \"Users: owner can update self-safe\" on public.users", "create policy \"Users: owner can select self\" on public.users for select to authenticated using (auth.email() is not null and lower(email) = lower(auth.email()))", "create policy \"Users: owner can update self-safe\" on public.users for update to authenticated using (auth.email() is not null and lower(email) = lower(auth.email())) with check (auth.email() is not null and lower(email) = lower(auth.email()))"], "search-terms": ["public.users", "verification_codes", "drop type app.code_purpose", "trigger set_updated_at", "row level security policies", "create schema in public", "drop legacy tables"], "state": 2, "file_id": 19, "knowledge_revision": 82, "git_revision": "", "hash": "9bc031e6ebc0ab3550f9d0e4877d29f2", "format-version": 4, "code-base-name": "authentication_backend", "filename": "authentication_backend/assets/supabase_users_unified.sql", "revision_history": [{"82": ""}]}